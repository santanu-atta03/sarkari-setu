Plan for Job Posting API:

1. Create Mongoose Job model with fields: title, state, eligibility, dates, slug
2. Auto-generate slug from title using a library or middleware
3. Create CRUD API routes in Express (Create, Read, Update, Delete)
4. Add validation for all fields
5. Add SEO metadata handling
6. Test API endpoints using Thunder Client or Postman
7. Integrate API with Next.js frontend job pages