Feature: SEO & Metadata

Description:
- Server-side rendering (SSR) and static generation (SSG)
- Dynamic meta tags for each page (title, description)
- Clean URLs (e.g., /ssc-gd-eligibility-2026)
- Sitemap generation
- Schema markup for job postings
- Fast loading with optimized images